from django.apps import AppConfig

class InterstoreAppConfig(AppConfig):
    name = "crm" # Здесь указываем исходное имя приложения
    verbose_name = "ЗАЯВКИ" # А здесь, имя которое необходимо отобразить в админке