from django.apps import AppConfig

class InterstoreAppConfig(AppConfig):
    name = "content" # Здесь указываем исходное имя приложения
    verbose_name = "КОНТЕНТ" # А здесь, имя которое необходимо отобразить в админке